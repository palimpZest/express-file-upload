extends layout

block content
    h1.text-center Uploadez vos images!
    hr
    form(method='POST', enctype='multipart/form-data' action='/monupload' accept='image/png')
        input(type='file', name='monfichier' multiple='multiple')
        br
        button.btn-primary(type='submit')  Envoyer
    hr
    h4.text-left Tip: to select multiple files, hold down the CTRL or SHIFT key while selecting.
